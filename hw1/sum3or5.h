// .h file is header file where functions are declared that
// are implemented in corresponding C file.

// extern indicates function is implemented 
// external in another file

/* 
 * Sum 3or5 takes an integer as a paremater and then adds together all the multiples of
 * 5 and 3 that are under the input number and adds them up. 
 */
extern int sum3or5(int n);